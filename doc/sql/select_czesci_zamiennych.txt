SELECT * FROM PRACE_NAPRAWCZE p LEFT JOIN PRACE_NAPRAWCZE_CZESCI pc ON p.ID_PRACY = pc.ID_PRACY LEFT JOIN CZESCI_ZAMIENNE c ON pc.ID_CZESCI = c. ID_CZESCI LEFT JOIN PRODUCENCI pr ON c.ID_PRODUCENTA = pr.ID_PRODUCENTA LEFT JOIN KATEGORIE k ON c.ID_KATEGORII = k.ID_KATEGORII WHERE p.ID_NAPRAWY = 50